/** Cookie non httpOnly : présence = session possible côté navigateur (jeton en sessionStorage). */
export const AUTH_SESSION_COOKIE = "otp_hora_auth";

/** Session portail entreprise (JWT entreprise ou clé API côté UI avancée). */
export const AUTH_ENTERPRISE_SESSION_COOKIE = "otp_hora_enterprise_auth";
